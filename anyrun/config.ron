Config(
    x: Fraction(0.5),
    y: Fraction(0.25),
    width: Fraction(0.20),
    hide_icons: false,
    ignore_exclusive_zones: true,
    layer: Overlay,
    hide_plugin_info: true,
    close_on_click: true,
    show_results_immediately: true,
    max_entries: Some(6),
    plugins: [
        "/run/current-system/sw/lib/libapplications.so",
        "/run/current-system/sw/lib/librink.so",
        "/run/current-system/sw/lib/libwebsearch.so",
    ],
)
